/** Status codes the client branches on by name rather than by number. */

/** The one extension per session was already spent. */
export const HTTP_CONFLICT = 409

/** Sending was throttled — a normal outcome, not a connection failure. */
export const HTTP_TOO_MANY_REQUESTS = 429
